class list
{
   private:
   node*head;
   node*tail;
public:
list()
{
head=NULL;
tail=NULL;

  }
}
void createnode
(int value)
{
node* temp=new node;
temp->data=value;
temp->next=NULL;
if
(head==NULL)
head=temp;
tail=temp;
temp=NULL;
}
else
{tail->next=temp;
tail=temp;

 }
}
